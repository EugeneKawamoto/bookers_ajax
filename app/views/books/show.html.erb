  <% if flash[:notice] %>
    <div class="flash">
      <%= flash[:notice] %>
    </div>
  <% end %>

<%= render 'users/sidemenu', user: @user, book: @book %>

  <p><strong>Tilte:</strong>
    <%= @book.title %></p>
  <p><strong>Body:</strong>
    <%= @book.body %>