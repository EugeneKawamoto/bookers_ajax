  <% if @book.errors.any? %>
    <div id="error_explanation" class="alert alert-danger">
      <ul>
        <% @book.errors.full_messages.each do |message| %>
          <li><%= message +"error" %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<%= render 'users/sidemenu', user: current_user, book: @book %>
<h1>Books#index</h1>
<p>Find me in app/views/books/index.html.erb</p>
